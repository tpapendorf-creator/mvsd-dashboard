{
  "name": "mvsd-dashboard",
  "version": "1.0.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "build": "npm --prefix constellation install && npm --prefix constellation run build",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
